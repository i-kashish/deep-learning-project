services:
  - type: web
    name: digit-recognizer
    env: python
    buildCommand: pip install -r digit-recognizer/requirements.txt
    startCommand: streamlit run digit-recognizer/app.py --server.port=$PORT --server.address=0.0.0.0
